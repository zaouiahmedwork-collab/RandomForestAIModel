# RandomForestAIModel
Machine Learning Project


In order to run this model u need to :
have python installed
have pandas installed (run this command to install : pip install pandas)
have sklearn installed (run this command to install : pip install pip install scikit-learn)

