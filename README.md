# RandomForestAIModel
Machine Learning Project

--use an editor if possible 

#In order to run this model u need to :  

1.have python installed  

2.have pandas installed (run this command on a terminal to install)  : pip install pandas)  

3.have sklearn installed (run this command on a terminal to install : pip install pip install scikit-learn)  


#to have the project locally on your machine :  

1.open an empty folder then  

2.use the command between quotes  on a terminal (git installed on your machine is needed)  : "git clone https://github.com/zaouiahmedwork-collab/RandomForestAIModel.git" 

1.or simply download the project as Zip then extract if the 2 privious steps was tough to execute
