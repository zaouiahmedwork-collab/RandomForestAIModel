# RandomForestAIModel
Machine Learning Project


In order to run this model u need to :
have python installed
have pandas installed (run this command on a terminal to install : pip install pandas)
have sklearn installed (run this command on a terminal to install : pip install pip install scikit-learn)

to have the project locally on your machine :
open an empty folder then
use this command on a terminal : git clone https://github.com/zaouiahmedwork-collab/RandomForestAIModel.git
or simply download the project as Zip then extract
